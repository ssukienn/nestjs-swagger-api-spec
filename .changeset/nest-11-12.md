---
"nestjs-swagger-api-spec": major
---

Support NestJS 11 and 12 (and matching `@nestjs/swagger` majors). Drop NestJS 10 and `@nestjs/swagger` 7/8.

`ApiSchema` and `ApiResponseProperty` are excluded from `ApiOptions` because they apply to DTOs, not controllers or handlers.
